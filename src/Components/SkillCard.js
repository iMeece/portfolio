const SkillCard = (props) => {

    // Should take an image and title for className

    return (
        <div className='skill-card'>
            { props.image }
        </div>
    );
}
 
export default SkillCard;